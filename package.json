{
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --config vite.config.mjs",
    "build": "vite build --config vite.config.mjs && node scripts/generateRelease.mjs",
    "upload": "npm run build && node scripts/uploadRelease.mjs"
  },
  "devDependencies": {
    "vite": "^5.4.19"
  }
}
